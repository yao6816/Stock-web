// Stock data array
let stocks = [];

// APi key for Alpha Vantage (IMPORTANT: This is a demo key with limitations)
//For a real application, use your own key and consider a backend proxy to protect it.
const API_KEY = '';

let notificationTimeout;

// Function to show notifications
fun showNotification(message, type = 'info', duratiion = 5000){
    const notification = document.getElementById('notification');
    const notificationMessage = document.getElementById('notification-message');
    const closeBtn = document.getElementById('close-notification');
    
    if (!notification = )
}
